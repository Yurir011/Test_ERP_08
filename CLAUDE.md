# 커밋 메시지 규칙

버전 형식: `V.0.01.NNN <변경사항 요약>`

- 앞자리 `V.0.01.`은 고정하고, 커밋마다 마지막 숫자(`NNN`)만 올린다.
- 버전 번호 뒤에 이번 커밋에서 무엇이 바뀌었는지 한 줄(또는 여러 줄) 요약을 이어서 적는다.
- 예: `V.0.01.101 변경사항 요약`

## 커밋 전 History.md 갱신

커밋을 만들기 전에 항상 `History.md`에 이번 변경사항을 시간순으로 정리해서 추가한 다음 커밋한다. (커밋 메시지 요약과 별개로, 세션 전체의 작업 흐름을 남기는 문서이므로 매 커밋마다 갱신)

## 커밋 후 항상 push

커밋을 만들면 항상 이어서 `git push`까지 완료한다. (로컬에만 남겨두지 않고 원격 저장소에도 바로 반영)
