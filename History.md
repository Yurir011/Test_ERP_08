# 작업 히스토리

날짜: 2026-08-27 (한 세션 내 진행 순서 기준)

## 1. 프로젝트 파악

폴더 전체를 읽고 구조 파악: PyQt5 기반 사내 문서 발행 프로그램(`Test_ERP_08`). 견적서/거래명세서/공문서 3종을 컴팩트 입력창 → 풀페이지 확인/저장창 흐름으로 작성하는 구조를 확인.

## 2. 전체 코드 리뷰

`main.py` 전체를 검토해 문제점 정리:
- **견적서/거래명세서 문서번호 이중 채번** — 컴팩트 창에서 보여준 번호를 풀페이지 창이 무시하고 재채번, 시퀀스 번호가 매번 하나씩 낭비됨
- 풀페이지 창의 문서번호 입력란이 읽기전용이 아니라 파일 경로에 그대로 쓰임(경로 조작 가능성)
- 리스트 엑셀이 다른 프로그램(엑셀)에서 열려있으면 저장이 부분 실패하는데 사용자에겐 "완전 실패"로만 보임
- 실제 발행 문서 파일이 git에 커밋되고 있음
- 엑셀 수식 인젝션 가능성
- `requirements.txt` 없음, 로컬 브랜치가 origin보다 1커밋 뒤처짐 등 기타 항목

## 3. 버그 수정 1 — 문서번호 이중 채번

`Quotation2Window`/`DeliveryNote2Window`가 `prefill`의 번호/날짜를 쓰도록 수정(`Doc2Window`는 이미 올바르게 처리 중이었음).

## 4. 버그 수정 2 — 공문서 Go 버튼을 눌러도 Doc2가 안 열림

실제 클릭 이벤트로 재현한 결과, 두 가지 원인 발견 및 수정:
- 멀티모니터 환경에서 새 창이 화면 밖에 위치할 수 있었음 → `move_centered_on()` 헬퍼로 항상 화면 안에 clamp
- `Doc2.ui`의 실제 위젯 배치가 `.ui`에 선언된 창 너비보다 넓어서 스크롤로도 닿지 않는 영역이 잘림 → `make_window_scrollable()`이 `childrenRect()` 기준으로 실제 크기를 계산하도록 수정, 가로 스크롤바도 대비

## 5. 컴팩트 입력폼 스타일 조정

수량/단가 칸 오른쪽 정렬 + 칸 높이 조정(46→55, 사용자가 `DeliveryNote.ui`에서 이미 늘린 값에 맞춤). 구현 중 `setAlignment()`가 `textChanged`를 재발생시켜 무한 재귀로 앱이 즉시 죽는 버그를 테스트로 발견, 가드 추가해 해결.

## 6. UI 변경사항 반영 (Designer에서 사용자가 직접 수정)

- **DeliveryNote2.ui**: 품목 8줄→6줄 축소 반영 (`DELIVERY_ITEM_ROWS` 수정), 컴팩트 폼 최대 행수를 문서 유형별로 분리(`max_item_rows`)
- **Doc.ui/Doc2.ui**: "문서참조" 버튼(`Go_pushButton_2`) 삭제 반영, 관련 죽은 코드(`load_reference_document`, `parse_saved_doc_*`) 제거. 결재 아이콘 경로가 루트를 가리키던 버그(`ICON/` 폴더로 수정)도 같이 발견해 수정
- **Quotation2.ui**: 같은 방식으로 품목 8줄→6줄 축소 반영

## 7. Request/Save/Print 버튼 기능 확인

- `Save_pushButton`은 이미 정상 동작 확인
- `Print_pushButton`에 인쇄 대화상자(`QPrintDialog`) 연결 — `common.py`(당시엔 `main.py`)에 `print_widget()` 추가
- `Request_pushButton`은 사용자 요청에 따라 미구현 상태로 보류

## 8. 버그 수정 3 — letterhead 필드 세로 정렬 깨짐

회사 정보란(대표/등록번호/종목/전화/이메일 등)이 줄바꿈 모드 기본값 때문에 텍스트가 위로 쏠려 보이는 문제 발견. 해당 필드들을 한 줄 모드로 전환해 `center_text_vertically()`가 올바른 계산 경로를 타도록 수정. (작업 중 `git stash` 실수로 변경사항이 잠깐 스태시에 들어갔다가 즉시 복구한 해프닝 있었음)

## 9. 시스템적 개선 — 전역 예외 처리

Designer에서 `.ui`를 수정할 때마다 위젯 참조가 깨져 "버튼을 눌러도 반응 없음" 현상이 반복되는 근본 원인 해결: PyQt가 슬롯 내부 예외를 콘솔에만 찍고 조용히 삼키던 것을 `sys.excepthook`으로 가로채 오류 팝업을 띄우도록 변경. 이후로는 어떤 위젯이 문제인지 즉시 화면에서 확인 가능.

## 10. 모듈 분리 리팩토링

1330줄짜리 단일 `main.py`를 문서 유형별로 분리:
- `common.py` — 세 문서가 공유하는 헬퍼 + `CompactItemFormWindow` 베이스 클래스
- `quotation.py` / `delivery_note.py` / `doc.py` — 각 문서 유형 (서로 참조하지 않도록 설계)
- `main.py` — `HomeWindow`와 앱 진입점만 남김 (1330줄 → 96줄)

실제 클릭 이벤트로 세 문서 전체 흐름(입력→Go→저장)을 재현해 예외 없음을 검증.

## 11. 문서화

- `refer.md` — 리팩토링 구조/의존 관계 설명
- `README.md` — 프로젝트 개요, 기능, 실행 방법, 필요 패키지, 파일 구조로 새로 작성 (기존 파일이 UTF-16으로 깨져 있던 것을 UTF-8로 재작성하며 인코딩 문제도 같이 해결)

## 12. 커밋 규칙 확립 및 첫 커밋

`CLAUDE.md`에 커밋 메시지 규칙 저장(`V.0.01.NNN <변경사항 요약>`, 마지막 숫자만 증가). 이번 세션 변경사항 전체를 `V.0.01.101` 커밋으로 반영.

- 커밋: `9a96659 V.0.01.101 견적서/거래명세서/공문서 모듈 분리 및 다수 버그 수정`
- 참고: 로컬 브랜치가 `origin/main`(`af1a5ce V.0.01.002`)보다 1커밋 뒤처진 상태라, push 전 `git pull`/rebase로 합치는 작업이 필요함 (아직 미push).
